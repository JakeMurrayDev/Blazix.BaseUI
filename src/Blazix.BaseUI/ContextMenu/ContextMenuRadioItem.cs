namespace Blazix.BaseUI.ContextMenu;

using Blazix.BaseUI.Menu;

/// <summary>
/// A context menu item within a radio group that represents a single selectable option.
/// Renders a <c>&lt;div&gt;</c> element.
/// </summary>
/// <typeparam name="TValue">The type used to identify the radio item.</typeparam>
public class ContextMenuRadioItem<TValue> : MenuRadioItem<TValue>;
